# PostgreSQL connection (optional!).
# Without DATABASE_URL the app runs entirely on the bundled offline packs in
# public/bible. With it, opened chapters are cached in Postgres and search
# uses the database.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
